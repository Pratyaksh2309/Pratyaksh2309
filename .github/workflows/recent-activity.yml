name: Update README with recent activity
on:
  schedule:
    - cron: "30 18 * * *"   # 00:00 IST (18:30 UTC, previous day)
    - cron: "30 6 * * *"    # 12:00 IST (06:30 UTC)
  workflow_dispatch:         # allow manual runs
permissions:
  contents: write
jobs:
  update-activity:
    runs-on: ubuntu-latest
    steps:
      - uses: jamesgeorge007/github-activity-readme@v0.4.5
        with:
          COMMIT_MSG: "chore: update recent activity (12h)"
